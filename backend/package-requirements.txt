# GENERATED BY AUGMENT (local-only): do not commit automatically. review before commit.
# Local install requirements for streaming voice prototype (backend only)

# Core
wrtc

# If running on Node < 18 (no global fetch)
node-fetch

# Optional helper if you prefer to avoid child_process.spawn
# cross-spawn

# System dependency (required)
# - ffmpeg must be installed and available on PATH

